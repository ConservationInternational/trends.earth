# Trends.Earth API Configuration
# Copy this file to .env and update with your actual credentials

# API Base URL (update if using a different environment)
API_BASE_URL=https://api.trends.earth

# Authentication credentials
USERNAME=your_username
PASSWORD=your_password

# Optional: API Key if your setup uses API keys instead of username/password
# API_KEY=your_api_key_here

# Environment setting (dev, staging, prod)
ENV=dev
